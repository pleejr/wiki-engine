# {{WIKI_NAME}} — the brain

{{BRAIN_CAP}} second brain (LLM-Wiki / Karpathy pattern). **This repo is the single durable brain.** Claude Code's native per-project memory is *raw scratch*; curated truth lives here, in git.

The reusable machinery (skills, node model, router, safety rules) is the **wiki-engine** submodule at `engine/`, pinned per bump. This file carries only the {{BRAIN}} boundary/identity and imports the engine's generic router below.

## Boundary (non-negotiable)

- `brain: {{BRAIN}}`. **No secrets** (keys, tokens, credentials). Keep this vault to the `{{BRAIN}}` brain — crossover to `{{OTHER}}` is a deliberate manual export only. Git identity `{{GIT_EMAIL}}`.

## Engine (imported)

Generic context router, working style, and the hard no-`claude`-in-hooks safety rule:

@engine/CLAUDE.md

Full conventions + node model: **`engine/SCHEMA.md`**. Navigation: **`index.md`** · history: **`log.md`**.
