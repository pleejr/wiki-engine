# {{WIKI_NAME}}

{{BOUNDARY_CAP}} knowledge base + **single durable vault** (LLM-Wiki / Karpathy pattern). Plain markdown — open as an Obsidian vault. Maintained **in-session by Claude Code**. Reusable machinery is the **wiki-engine** submodule at `engine/`.

- Agent entry point: [CLAUDE.md](CLAUDE.md) (thin router; imports the engine)
- Day-to-day usage: [engine/USAGE.md](engine/USAGE.md) · conventions + node model: [engine/SCHEMA.md](engine/SCHEMA.md)
- Navigation: [index.md](index.md) · history: [log.md](log.md)
- `boundary: {{BOUNDARY}}`. **No secrets.** Single git identity.

Fresh clone: `git clone --recurse-submodules <url>` (or `git submodule update --init`) or `engine/` is empty.
