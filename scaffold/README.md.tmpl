# {{WIKI_NAME}}

{{BRAIN_CAP}} knowledge base + **single durable brain** (LLM-Wiki / Karpathy pattern). Plain markdown — open as an Obsidian vault. Maintained **in-session by Claude Code**. Reusable machinery is the **wiki-engine** submodule at `engine/`.

- Agent entry point: [CLAUDE.md](CLAUDE.md) (thin router; imports the engine)
- Conventions + node model: [engine/SCHEMA.md](engine/SCHEMA.md)
- Navigation: [index.md](index.md) · history: [log.md](log.md)
- `brain: {{BRAIN}}`. **No secrets.** Single git identity.

Fresh clone: `git clone --recurse-submodules <url>` (or `git submodule update --init`) or `engine/` is empty.
