# {{WIKI_NAME}} — Index

{{BOUNDARY_CAP}} knowledge vault + single durable vault. **Start here**, then follow only relevant `[[links]]`. Maintained in-session by Claude Code (no orchestrator); conventions in `engine/SCHEMA.md` (the wiki-engine submodule).

## Memory

_none yet_ — curated `preference` / `decision` / `lesson` notes in `memory/`; distilled by `checkpoint`.

## Concepts

_none yet_

## Entities

_none yet_

## Repos

_none yet_ — add with the `wiki-repo` skill (one repo per run, git-ref provenance).

## Projects

_none yet_ — `projects/` pages + `log.md` entries; updated by the `checkpoint` skill.

## Skills

_Provided by the **wiki-engine** submodule; generated from `engine/skills/*/SKILL.md` by `engine/bin/gen-skills-index.sh` (skills symlinked to `~/.claude/skills/`). Do not edit the block below by hand._

<!-- skills:start -->
- **checkpoint** _(active)_ — end-of-session: update project page + `log.md`, distill memory. In-session only.
- **wiki-context** _(active)_ — index-first, lazy context router with repo freshness check.
- **wiki-onboard** _(active)_ — seed a freshly-scaffolded vault from existing memories, repos, and skills.
- **wiki-repo** _(active)_ — ingest/refresh one repo's wiki page with git-ref provenance.
<!-- skills:end -->

## Comparisons / Queries

_none yet_

## Raw sources

_none yet_ — immutable captures in `raw/` (`articles/`, `papers/`, `transcripts/`, `assets/`).
