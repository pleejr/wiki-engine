# Obsidian state — the engine is machinery, not a vault; never track it here
.obsidian/

# OS cruft
.DS_Store

# Python bytecode cache — regenerated by running the RAG tools; never track it
__pycache__/
*.pyc
