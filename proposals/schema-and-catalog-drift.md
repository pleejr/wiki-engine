---
slug: schema-and-catalog-drift
outcome: partially-accepted
reason: "partially-accepted — all five observations reproduced at HEAD by the report's own one-line controls (12 skills vs 4 listed; used_by written by 12 and read by 0; the three identifier classes at their seven sites; five executable bare engine/bin paths among ten grep hits; three verbs documented against six dispatched; shellcheck 0.11 reporting the same ten warnings — plus seven more in adopt.d/, the sibling the report did not measure, fixed in the same pass). Suggested fixes (a), (c), (d), (e), (f) and (g) taken as proposed. (b) decided as DROP: nothing reads used_by, the catalog line has no such concept, and lint.sh enumerates no allowed skill keys, so removal needs no migration. PARTIAL on (h), and that is the declined half: the denylist-in-lint-docs shape is not built, for a technical reason rather than taste — a static list of the given names and consumer slugs known to have leaked would itself carry those identifiers into the public repository the gate exists to keep them out of. The report's own falsification of the user.name-first-token idea is recorded and agreed. The PUSH question was read past line 83 as instructed: the push keys on REMOTE_URL / CREATE_REMOTE at lines 143-152, so PUSH was dead and is removed rather than wired."
received: 2026-09-03
---

HANDOFF — engine defect report slug: schema-and-catalog-drift boundary: generic (engine-domain; contains no consumer-private context)

Title: SCHEMA.md, a script header and skill text describe behaviour the code no longer has; the generic engine carries consumer identifiers in seven places; five skill commands use paths that resolve only from one cwd

Engine version: v1.73.4 — the tag this vault is pinned to. Still live at that pin: every item below was re-measured at the pinned commit on 2026-09-03. The pin moved v1.73.3 -> v1.73.4 since this block was first drafted; that range touched only `CHANGELOG.md`, `PROPOSALS.md`, `proposals/` and `scaffold/rag-requirements.txt`, so no subject file here changed and all five items still reproduce. The pin sits one commit behind the engine's `origin/main` (a ledger stamp), so it is not equal to it.

Observed (five independent items, each with its own control):

1. Skills catalog and a dead frontmatter field. `SCHEMA.md` lines 69-76 ("Skills") list four skills — `wiki-repo`, `wiki-context`, `checkpoint`, `wiki-onboard`; `ls skills/` → 12. `SCHEMA.md` line 23 and line 65 say the catalog scan reads `used_by`; `grep -rn used_by bin/` → no match, while all 12 `skills/*/SKILL.md` carry `used_by: []` on line 7. The field is written by every skill and read by nothing; the SCHEMA sentence describing it cannot be checked against any script. Separately, `SCHEMA.md` lines 113-141 (~1500 words) are `rag-capture.sh` design narrative in a file that is one link away from every session.

2. Consumer identifiers in the public engine — **seven locations in five files**, in three identifier classes. (i) A human's first name is the example value in `SCHEMA.md` line 56 (`e.g. <name> | claude`), `bin/verify-status.sh` line 9 (`by: <name>`), and `skills/verify/SKILL.md` line 37 (`by: <<name> | claude | whoever confirmed>`). (ii) `bin/lint.sh` lines 28-29 name a specific consumer vault and one of its projects. (iii) A consumer repo slug appears in `SCHEMA.md` line 40 (as an example wikilink) and in `skills/verify/SKILL.md` lines 49 and 51, whose worked example also names that repo's deployment objects. Control, and where the first draft of this report was wrong: one grep for the name and the vault slug returns only **four** of the seven — it cannot see the repo slug, which is a third identifier with no lexical overlap with the other two. The complete control is three greps, one per class, each over `*.md` and `*.sh` excluding `CHANGELOG.md` and `proposals/`; a single-pattern sweep reporting "clean" is a broken probe, not a negative. `bin/lint.sh` line 32 states the engine's own rule — it names no consumer's strings — and `skills/engine-proposal/SKILL.md` line 141 states the repository is public. Why the boundary scan did not catch it, re-measured 2026-09-03 by reading `do_scan`'s own derivation: the needles are the vault directory basename, the remote `org`, `repo` and `org/repo` slugs, `user.name`, and `user.email` — nothing else, and each must appear as a literal. On the vault where this was found `user.name` is an account handle, not a person's name, so the leaked given name is in **none** of the needles and no variant of the existing derivation can reach it. The scan is doing exactly what it documents; the gap is that a human given name is not derivable from a vault at all. It is also worth stating what the scan *does* catch, so its clean verdict is not read as broader than it is: a home path in the same block is flagged.

3. Relative `engine/bin/…` paths. `skills/verify/SKILL.md` lines 27 and 40, `skills/wiki-onboard/SKILL.md` lines 31 and 33, `skills/checkpoint/SKILL.md` line 65 write `engine/bin/<tool>` bare as a command to run. Every other command in those files, and in `wiki-context`, `crossover`, `engine-proposal`, uses `$WIKI_PATH/engine/bin/…`. `verify` runs against a repo clone at `$UPKEEP_REPOS_ROOT/<repo>` (line 23) where `engine/bin/` does not resolve; `checkpoint` §0 line 22 says engine tooling runs from canonical because the submodule is absent in a session worktree — exactly where a bare relative path fails. Measured in a session worktree on 2026-09-03: `ls <worktree>/engine` is empty, so all five bare paths resolve to nothing there.

4. `bin/engine-proposal.sh` header. Lines 16-20 ("Subcommands") and 41-43 ("Usage") list `scan | stash | status` only. The dispatch at lines 704-711 accepts `scan | stash | submit | push | queue | status`; `do_stash` (line 151) prints "RETIRED" (line 161). The two verbs that publish are the two the header omits.

5. `shellcheck -S warning` over `bin/*.sh` (shellcheck 0.11.0): 10 warnings in 5 files; `bash -n` clean on all 43 scripts; `python3 -m py_compile` clean on the three `.py`.
   - `new-wiki.sh:60-61` SC2034 — `PUSH=1` set in both `--remote` and `--create-remote` branches, never read. Either the push step keys on `REMOTE_URL`/`CREATE_REMOTE` and `PUSH` is dead, or a push step is missing. NOT VERIFIED which — read past line 83 before deciding.
   - `crossover.sh:506` SC2046 — `$(vault_grep_excludes)` unquoted on purpose (it returns a space-separated `--exclude-dir=…` list, `wiki-root-lib.sh:173-178`). Correct as written; needs a `# shellcheck disable=SC2046` with the reason, or an array from the lib.
   - `engine-proposal.sh:470,474` SC2178/SC2128 — `f` is a `local` array in `do_status` (`read -ra f`, line 220) and a `local` string in `do_submit`. False positive; rename one.
   - `engine-proposal.sh:541` SC2155 — `local marker="$(…)"` masks the subshell exit; cosmetic under `set -uo pipefail` without `-e`.
   - `engine-proposal.sh:600` SC2034 — `local fm_get; fm_get() {…}`: the variable is unused, the function is used. Drop the `local`.
   - SC2088 at `new-wiki.sh:83`, `session-preflight.sh:85`, `skill-sources.sh:30`, `wire-machine.sh:45` — `case … in "~/"*)` is a deliberate match on a literal tilde. False positives; add a directive.

Expected:
  - `SCHEMA.md` describes the catalog mechanism and only fields a script reads; the Skills section does not enumerate a stale subset.
  - The engine carries no consumer's human id, vault name, project name or repo name outside `CHANGELOG.md` and `proposals/`.
  - Every command in a skill body resolves from any cwd the skill can run in.
  - A script header lists the verbs its dispatch accepts and marks retired ones.

Reproduction (generic):
  1. `ls skills | wc -l` vs the count of skills named in `SCHEMA.md` §Skills (lines 69-76) → 12 vs 4.
  2. `grep -rn used_by bin/` → empty; `grep -c '^used_by' skills/*/SKILL.md` → 12 ones.
  3. `grep -rn -i -e '<first-name>' -e '<consumer-vault-slug>' -e '<consumer-repo-slug>' --include='*.md' --include='*.sh' . | grep -v -e CHANGELOG -e '^./proposals/'` → the seven locations above. Identifiers redacted here; derive the first two from `git log --format=%an` on the engine and from `bin/lint.sh:28`, and the third from `SCHEMA.md:40` / `skills/verify/SKILL.md:49`. Dropping the third pattern returns four hits and looks like a smaller defect than it is.
  4. `grep -n '[^$/]engine/bin' skills/*/SKILL.md` → ten lines across six files. Five are executable commands and are the defect: `verify` 27 and 40, `wiki-onboard` 31 and 33, `checkpoint` 65. The other five are not: `crossover` 14 and `engine-proposal` 14 are prose naming a script's home, and `checkpoint` 55, `wiki-context` 17 and 19 already carry `$WIKI_PATH` elsewhere in the same sentence. The grep does not separate the two, so read each hit before fixing it — a blanket rewrite would edit prose.
  5. `sed -n 16,20p bin/engine-proposal.sh; sed -n 704,711p bin/engine-proposal.sh` → three verbs documented, six dispatched.

Failure shape: fail-open in every item — a reader follows the stale statement (writes a field nothing reads, runs a documented verb list missing the two that publish, runs a command that resolves only from one cwd, copies a `by:` example that names a real person) and nothing reports the mismatch. Item 2 is additionally a boundary defect in a public repository, which is the failure the engine's own scan and lint rule exist to prevent.

Already ruled out:
  - That `used_by` is read by a consumer-side generator — the catalog in a consumer's `index.md` is rendered by the engine's `bin/gen-skills-index.sh`, which does not read it.
  - That the identifiers are confined to history — `CHANGELOG.md` and `proposals/` were excluded from the grep and the five hits remain in live docs and scripts.

Suggested fix (HOLD LOOSELY — may be wrong): (a) `SCHEMA.md` Skills section → "see the generated catalog in the vault's `index.md`" plus one line per skill family (vault lifecycle / engine-dev loop / verification). (b) Either make `gen-skills-index.sh` render `used_by` or drop the field from `SCHEMA.md` and the 12 frontmatters — check first whether `lint.sh`'s frontmatter validity check enumerates allowed keys (not read for this report). (c) Move the `rag-capture.sh` narrative out of `SCHEMA.md` into the script header or `USAGE.md`; keep the hook JSON (127-133), the `timeout` rule (135, one clause), the boundary refusal (139, one clause), and the git-ignored/per-machine fact (117). (d) Replace the human id with `<human-id>` in the three `by:` examples; replace `lint.sh:28-29` with the generic reason (gates held at zero so no backlog normalizes); rewrite or drop the `verify` §Example — body line 12 already states the fresh-but-wrong principle. (e) Prefix the five executable relative paths with `$WIKI_PATH/`; leave the prose mentions alone. (f) Header lines 16-20 and 41-43 list `submit`, `push`, `queue`; mark `stash` retired. (g) One shellcheck housekeeping PR: directives for the intentional patterns, the two renames, and a decision on `PUSH`. (h) The first draft of this report suggested teaching `engine-proposal.sh scan` to also match the first token of `user.name` case-insensitively. **That hypothesis is falsified — do not build it.** Measured 2026-09-03: on the vault where the leak was found `user.name` is an account handle, so its first token is the handle and the given name is unreachable from it. If a check is wanted at all, the only shape that can work is a static denylist the engine keeps for its own text (the given names and consumer slugs already known to have leaked), checked by `lint-docs.sh` over `*.md` and `bin/*.sh` excluding `CHANGELOG.md` and `proposals/` — a lint on the engine's own prose, not an addition to the consumer-side scan, which cannot derive what it is being asked to find. Decide whether that is worth a gate; the four items above are one-time fixes either way.

Redactions: the human first name, the consumer vault slug, the consumer project name and the consumer repo/deployment names at the cited lines are described by location and shape only; they are recoverable from the files themselves. Nothing was dropped for being awkward to scrub — every redacted string is named by its location so a gap reads as deliberate.

Instruction to engine-dev: reproduce first — every item is a one-line grep or sed at the pin. Items (d) and (e) are contained fixes with no design question; do them first, and note item 2 needs three greps rather than one to confirm it is fully closed. Item (b) is the only one with a choice (render or drop); decide it against `lint.sh`. Treat (h) as a hypothesis.
