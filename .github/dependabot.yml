version: 2
updates:
  # Keep the CI's own actions (checkout, setup-python, …) current — opens PRs.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
